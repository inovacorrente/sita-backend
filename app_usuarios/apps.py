from django.apps import AppConfig

class AppUsuariosConfig(AppConfig):
    """
    Configuração da aplicação app_usuarios para o Django.
    Define o nome da aplicação e outras configurações relacionadas.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'app_usuarios'
